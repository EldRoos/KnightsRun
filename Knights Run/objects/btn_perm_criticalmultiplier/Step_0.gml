// Genetix Studio
if store.gems >= store.cost_perm_critical_multiplier {image_index = store.color_scheme} else {image_index = 0}
if store.perm_critical_multiplier >= stat_cap {image_index = 0 maxxed = 1}

